import React from 'react';
import { ThemeSwitcher } from '../ThemeSwitcher';

const TheHeader = () => {
    return (
        <header>
            <ThemeSwitcher />
            header part
        </header>
    );
};

export default TheHeader;