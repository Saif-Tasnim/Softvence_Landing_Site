import React from 'react';

const SectionTitle = ({ title }: { title: string }) => {
    return <h1 className='uppercase text-[56px] text-primary font-extrabold text-center'>{title}</h1>;
};

export default SectionTitle;
